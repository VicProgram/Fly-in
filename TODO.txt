'''
TODO — Estado del proyecto Fly-in
Actualizado tras revisión contra el subject v1.4

===========================================================
OK — YA HECHO Y VERIFICADO
===========================================================

PARSER
- [OK] Se parsean zone=, color=, max_drones=, max_link_capacity= (en cualquier orden).
- [OK] Hub(name, x, y, zo_type, color, hub_type, max_drones) en el orden correcto.
- [OK] Validación de zo_type contra Valid_List.valid_zones (tipo inválido → error).
- [OK] Los colores aceptan cualquier palabra de una sola palabra (no se sobre-valida).
- [OK] No puede haber dos hubs en la misma coordenada (used_coords).
- [OK] No puede haber nombres de hub repetidos.
- [OK] No puede haber más de un start_hub ni más de un end_hub.
- [OK] Connection.__eq__/__hash__ con frozenset: a-b y b-a son la misma conexión.
- [OK] Nombres normalizados a minúsculas en hubs y en connection:.
- [OK] nb_drones: entero positivo, validado (1-499) sin crash.
- [OK] nb_drones: debe ser la primera instrucción válida del archivo (flag drones_parsed).
- [OK] Conexiones solo enlazan hubs ya definidos previamente (order importa).
- [OK] Una conexión no puede unir un hub consigo mismo.
- [OK] max_drones y max_link_capacity deben ser positivos (> 0).
- [OK] max_link_capacity mal formado → error controlado (try/except en connection:).
- [OK] Al terminar: se comprueba que existan start_hub y end_hub (parse_file).
- [OK] Red de seguridad genérica en parse_file (cualquier excepción → mensaje + exit(1)).
- [OK] Comentarios con '#' y líneas vacías se ignoran.
- [OK] El parser carga los 10 mapas (easy/medium/hard/challenger) sin error.

REQUISITOS TRANSVERSALES
- [OK] Python 3.10+.
- [OK] Proyecto orientado a objetos (todo son clases; main() es la excepción permitida).
- [OK] Type hints en casi todas las funciones/métodos (quedan pocos sueltos, ver paso 3).
- [OK] Makefile con las 4 reglas obligatorias (install, run, debug, clean) + lint/lint-strict.
- [OK] .gitignore presente.
- [OK] Los errores del parser salen por stderr con mensaje claro y salida controlada.

===========================================================
PENDIENTE — PASOS A SEGUIR EN ORDEN
===========================================================

=== FASE 0 — Limpieza rápida (cosas de 5 min que desbloquean el resto) ===

- [ ] 1. flake8 a cero: corregir los 6 errores actuales
      models.py:1 F401 'typing.List' importado y sin usar → quitar List del import.
      models.py:125,128,148 y parsing.py:57,140 E501 líneas > 79 caracteres → partir.
      Verificación: python3 -m flake8 --exclude=venv . → 0 errores.

- [ ] 2. Arreglar .gitignore: '__pycache/' es un typo, debe ser '__pycache__/'.
      Añadir también '*.pyc' por si acaso.
      Quitar los .pyc commiteados del repo (git rm --cached __pycache__/*.pyc)
      para que el árbol esté limpio.

- [ ] 3. Anotar los tipos que quedan sueltos (mypy --disallow-untyped-defs):
      models.py:150 'neightbors = []' → 'neightbors: list[tuple[Hub, Connection]] = []'
      models.py:167 'self.history: list' → 'self.history: list[str]'
      models.py:121 'self.used_coords: set' → 'self.used_coords: set[tuple[int, int]]'
      parsing.py:113 'nuevo_hub: Any = None' → 'nuevo_hub: Hub' (asignar dentro del match)
      y quitar el import de Any si sobra.

- [ ] 4. Makefile funcional en esta máquina:
      - 'lint' y 'lint-strict' usan 'uv run', pero 'uv' no está instalado y el
        'install' usa pip → cambiar a 'python3 -m flake8' / 'python3 -m mypy'.
      - 'clean' usa '$(VENV)' que no está definido → borrarlo o definirlo.
      - 'run' pasa un mapa: usar 'python3 fly_ing.py $(MAP)' con un MAP por defecto
        (p.ej. maps/easy/01_linear_path.txt).
      - 'install' debe instalar flake8, mypy y pytest (dev deps), o meterlas en
        requirements.txt. Probar 'make lint' hasta que pase limpio.

- [ ] 5. Docstrings en TODAS las clases y funciones (obligatorio, PEP 257):
      falta en: parse_hub_content, parse_line, print_avances (parsing.py);
      get_drone_info, add_hub, add_connection, get_neightbors, get_drones_in_hub,
      get_drones_in_con, can_move_hub, can_move_conn, get_move_costs, run, find_path
      (models.py); main (fly_ing.py).
      Estilo Google o NumPy: qué hace, params, returns.

=== FASE 1 — Corregir el modelo de costes (subject VII.3) ===

- [ ] 6. Valid_List.zone_costs está MAL (models.py:19-24):
      actual:  normal=2, restricted=5
      correcto: normal=1, priority=1, restricted=2, blocked=infinito.
      El coste es el del tipo de zona DESTINO, sin sumar base.

- [ ] 7. get_move_costs (models.py:186-198) suma base_cost + zone_cost → infla turnos.
      Debe devolver SOLO el coste de la zona de destino (1, 2, o bloqueado).
      Los checks de can_move_hub/can_move_conn NO son coste: van al motor de
      simulación, no al pathfinding. Revisar esta función y simplificarla.

=== FASE 2 — Pathfinding correcto (Dijkstra) ===

- [ ] 8. find_path está roto (models.py:207-240):
      - Bug de indentación: el 'if new_cost < min_cost...' (línea 233) queda FUERA
        del 'for' de vecinos → si un hub no tiene vecinos lanza NameError.
      - Usa queue.sort() + pop(0) → O(n^2). Cambiar a heapq.
      - Quitar el 'print(queue)' de debug (línea 238).
      - El continue con step_cost >= 999999 debe estar correctamente dentro del loop.

- [ ] 9. Desempate 'priority' (subject VII.1/VII.3): a coste igual, preferir la ruta
      que pase por más zonas priority (criterio de tiebreak del pathfinding).

- [ ] 10. Arreglar fly_ing.py:23: 'print(hub.name for hub in camino)' imprime un
      <generator object>. Cambiar a un join o recorrerlo de verdad.

- [ ] 11. Verificar: en easy/01_linear_path (3 aristas, todo normal, coste 1 c/u),
      find_path debe devolver [start, waypoint1, waypoint2, goal] con coste 3.

=== FASE 3 — Motor de simulación (EL NÚCLEO DEL PROYECTO) ===

- [ ] 12. Ocupación dinámica por turno: contadores reales de cuántos drones hay en
      cada hub y cada conexión en cada turno (no confundir con la capacidad máxima).

- [ ] 13. Bucle principal de turnos (Solver.run):
      1) por cada dron activo, decidir el movimiento que intenta (según find_path);
      2) validar el CONJUNTO de movimientos del turno:
         - las salidas de un hub liberan plaza en ese mismo turno (¡se evalúa
           primero liberar y luego ocupar!);
         - un hub debe tener capacidad libre tras las salidas para admitir entradas;
         - respetar max_link_capacity de conexiones.
      3) aplicar los movimientos válidos y actualizar posiciones;
      4) registrar el turno para la salida final;
      5) si quedan drones activos → turno siguiente; si no → terminar.

- [ ] 14. Zonas restricted (subject VII.3): movimiento de 2 turnos.
      - El dron abandona el hub origen de inmediato (libera plaza).
      - En el turno intermedio ocupa la CONEXIÓN (capacity de conexión).
      - Compromiso irreversible: una vez iniciado, DEBE llegar el turno siguiente.
      - Antes de iniciar el tránsito hay que saber si habrá sitio en el destino
        cuando llegue (decisión con 2 turnos de antelación).

- [ ] 15. Drones entregados: al llegar al end_hub dejan de ser activos (no ocupan,
      no aparecen en la salida). La simulación termina cuando no queda ninguno activo.

- [ ] 16. Distribución multi-ruta + anti-deadlock:
      - Repartir drones entre varias rutas cuando el grafo lo permita.
      - Espera estratégica cuando el movimiento no es posible.
      - Prioridad consistente para que siempre haya progreso (p.ej. el dron que
        lleva más turnos esperando, o el ID más bajo) y no haya deadlock.

=== FASE 4 — Formato de salida exacto (subject VII.5) ===

- [ ] 17. Una línea por turno, movimientos separados por espacios:
      - Movimiento a zona: D<ID>-<zona>
      - En tránsito hacia restricted: D<ID>-<conexion>
      - Drones que no se mueven: omitidos de la línea.
      - Sin líneas vacías intermedias, sin drones fantasma.
      - El último turno entrega a los últimos drones y termina.
      - Validar contra el ejemplo del subject (D1-roof1 D2-corridorA ...).

=== FASE 5 — Visual representation (subject VII.1, obligatorio) ===

- [ ] 18. Colorear la salida usando el color= de cada hub (ya tienes los ANSI en
      Valid_List.valid_colors) y mostrar ocupación por turno.
      Ideal: mini-mapa ASCII usando coordenadas x,y con los drones encima.
      Debe poder explicarse cómo la visual ayuda a entender la simulación.

=== FASE 6 — Métricas y tests ===

- [ ] 19. Métricas opcionales al final de la salida (sujeto, "secondary metrics"):
      drones movidos por turno, media de turnos por dron, coste total acumulado.
      (Opcionales pero recomendadas para el peer-review.)

- [ ] 20. Tests pytest (no se entregan pero te salvan la evaluación):
      - parser: mapa sin nb_drones primero, color de dos palabras, conexión a hub
        inexistente, capacidades 0, zona type inválido.
      - simulación: capacidad saturada, zona blocked en el único camino,
        restricted (2 turnos), deadlock imposible, drones entregados.
      - Verificación: python3 -m pytest.

=== FASE 7 — README.md en la raíz (subject VIII, obligatorio) ===

- [ ] 21. README.md en la RAÍZ del repo (ahora solo existe maps/README.md, que es
      otro documento de los mapas). Debe incluir:
      - Primera línea en cursiva: *This project has been created as part of the 42
        curriculum by <tu login>.*
      - Sección Description (objetivo + overview).
      - Sección Instructions (instalación y ejecución).
      - Sección Resources (referencias + cómo usaste IA y para qué).
      - Descripción del algoritmo (por qué Dijkstra, esperas, conflictos).
      - Documentación de la representación visual.
      Todo en inglés.

=== FASE 8 — Benchmarks (subject VII.7) ===

- [ ] 22. Ejecutar los 10 mapas y comparar turnos contra los targets:
      easy   01 ≤6   02 ≤8   03 ≤6
      medium 01 ≤12  02 ≤15  03 ≤12
      hard   01 ≤30  02 ≤35  03 ≤45
      (challenger 25 drones: batir 45 turnos es bonus, no obligatorio.)
      Aquí es donde se ve si la simulación y el pathfinding rinden.

=== FASE 9 — Revisión final ===

- [ ] 23. 'make lint' limpio (flake8 + mypy con las flags del Makefile).
- [ ] 24. Crear tus propios mapas rotos y raros (no solo los oficiales) y probarlos:
      el enunciado avisa que los mapas de evaluación pueden ser distintos.
- [ ] 25. Poder explicar en voz alta, sin mirar el código: parser, motor de
      simulación, pathfinding y visualización. Te pueden pedir una modificación
      pequeña en vivo durante la evaluación.

=== BONUS (solo si todo lo anterior está sólido) ===

- [ ] 26. Batir o igualar los targets de todos los mapas y resolver el challenger.
      No empezar esto hasta que la parte obligatoria esté probada de arriba a abajo.

===========================================================
NOTA SOBRE EL ORDEN
===========================================================
Fases 0-1 son rápidas y arreglan lo que ya existe.
Fase 2 deja un camino correcto para un dron solo.
Fase 3 es el grueso (motor de simulación con turnos y capacidades).
Fases 4-5 dan salida y visual (obligatorias).
Fase 8 solo tiene sentido con Fases 2-4 hechas.
'''
